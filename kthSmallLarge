#include<bits/stdc++.h>
vector<int> kthSmallLarge(vector<int> &arr, int n, int k)
{
	// Write your code here.
	sort(arr.begin(),arr.end());
	int i=k-1;
	int j=n;
	while(k--){
		j--;
	}
	vector<int>ans;
	ans.push_back(arr[i]);
	ans.push_back(arr[j]);
	return ans;
}
